node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.vite/

# Estado de runtime do servidor: snapshot das salas (snapshot.js) e contagens da
# telemetria (telemetria.js). Efemero e local — a copia duravel vive na BD.
server/.data/
